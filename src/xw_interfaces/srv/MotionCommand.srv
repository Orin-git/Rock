string command_id
# yaw degrees: +left/CCW, -right/CW
float32 angle_deg
# signed drive meters: +forward, -backward (turn completes first if angle!=0)
float32 distance_m
---
bool success
string message
