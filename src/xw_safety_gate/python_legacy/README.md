# Python legacy (disabled)

Former ament_python implementation of `xw_safety_gate` kept for reference only.

- Not installed / not launched.
- Active node is the C++ executable `safety_gate_node` (same package, node name, topics, parameters).
- Original entry point was `xw_safety_gate.safety_gate_node:main`.
