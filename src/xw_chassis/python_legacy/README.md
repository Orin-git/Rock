# Python legacy (disabled)

Former ament_python implementation of `xw_chassis` kept for reference only.

- Not installed / not launched.
- Active node is the C++ executable `chassis_node` (same name, topics, parameters).
- To compare protocol behavior: run `python3 test/test_serial_mcu.py` against files under `xw_chassis/` here.
